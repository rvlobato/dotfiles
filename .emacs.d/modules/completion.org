#+TITLE: Completion
#+PROPERTY: header-args:emacs-lisp :tangle yes

* Minibuffer and completion

#+begin_src emacs-lisp
(my/use-package vertico
  :init
  (vertico-mode 1))

(my/use-package orderless
  :custom
  (completion-styles '(orderless basic))
  (completion-category-defaults nil)
  (completion-category-overrides
   '((file (styles partial-completion)))))

(my/use-package marginalia
  :init
  (marginalia-mode 1))

(my/use-package consult
  :bind (("C-s"     . consult-line)
         ("C-c h"   . consult-history)
         ("C-x b"   . consult-buffer)
         ("C-x r b" . consult-bookmark)
         ("C-c k"   . consult-kmacro)
         ("M-y"     . consult-yank-pop)
         ("C-c m"   . consult-mode-command)
         ("M-s G"   . consult-grep)
         ("M-s R"   . consult-ripgrep)))

(my/use-package embark
  :bind (("C-." . embark-act)
         ("C-;" . embark-dwim)))

(my/use-package embark-consult
  :after (embark consult))

(my/use-package corfu
  :init
  (global-corfu-mode 1)
  :custom
  (corfu-auto t)
  (corfu-auto-delay 0.2)
  (corfu-auto-prefix 2)
  (corfu-preview-current nil))

(my/use-package cape
  :init
  (add-to-list 'completion-at-point-functions #'cape-dict)
  (add-to-list 'completion-at-point-functions #'cape-file)
  (add-to-list 'completion-at-point-functions #'cape-dabbrev)

  (add-hook 'prog-mode-hook (lambda () (add-to-list 'completion-at-point-functions #'cape-tex)))
  (add-hook 'text-mode-hook (lambda () (add-to-list 'completion-at-point-functions #'cape-tex))))

(my/use-package bookmark
  :ensure nil
  :custom
(bookmark-default-file "~/Templates/configs/emacs/bookmarks")
(bookmark-save-flag 1))
#+end_src
