#!/usr/bin/env bash

dbus-monitor "interface='org.freedesktop.portal.Settings', member='SettingChanged'" |
    while read -r line; do
	case "$line" in
	    *"prefer-dark"*) alacritty-themes Gruvbox-Dark;;
	    *"default"*) alacritty-themes One-Light;;
	esac;
    done
